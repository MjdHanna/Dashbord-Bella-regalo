import { IconAd } from '@tabler/icons-react';

const icons = { IconAd };

const adds = {
  id: 'adds-group',
  title: '',
  type: 'group',
  children: [
    {
      id: 'adds',
      title: 'Ads',
      type: 'item',
      url: '/adds',
      icon: icons.IconAd
    }
  ]
};

export default adds;
